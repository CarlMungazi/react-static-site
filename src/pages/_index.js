import React from 'react'
import { withRouteData , RouteData  } from 'react-static'

export default withRouteData (({ topics }) => (
  <div style={{ textAlign: 'center' }}>
    <h1>Welcome to React-Static</h1>
  </div>
))
